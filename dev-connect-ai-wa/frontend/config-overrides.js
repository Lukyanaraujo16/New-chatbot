/**
 * config-overrides.js - exigido pelo react-app-rewired.
 * Sem customizações; usa a configuração padrão do Create React App.
 */
module.exports = function override(config, env) {
  return config;
};
